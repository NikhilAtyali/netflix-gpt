// Mock Firebase for Jest tests
// This file will be automatically used by Jest when firebase.js is imported

// Mock auth object
export const auth = {
  currentUser: null,
};

// Mock app
const app = {};
export default app;

